namespace Shoegaze.LastFM
{
  /// <summary>
  /// Describes a type that can be used with
  /// <see cref="User.IUserApi.GetWeeklyChartAsync{T}(string, DateTime?, DateTime?, CancellationToken)"/>.
  /// </summary>
  public interface IChartable
  {
  }
}
